# Core (required for the local, zero-API-key demo via Ollama)
streamlit>=1.32
pandas>=2.0
numpy>=1.24
rank-bm25>=0.2.2
requests>=2.31
truststore>=0.9            # use the OS cert store (needed behind corporate SSL proxies)

# Cloud providers use plain HTTPS (Gemini) or lazy-imported SDKs — install only
# what you use:
# pip install anthropic     # for LLM_PROVIDER=anthropic (Claude)
# pip install openai        # for LLM_PROVIDER=openai and OpenAI embeddings
# (Google Gemini needs no SDK — it's called over REST via requests.)
